apiVersion: v1
kind: Service
metadata:
  name: relay-public
  namespace: aether-relay
  labels:
    app.kubernetes.io/name: relay
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: relay
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 31326
